<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Eat-Da-Burger!</title>

    {{!-- <link rel="stylesheet" href="../../public/assets/css/style.css" type="text/css" /> --}}
    {{!-- css does not work --}}
    <script src="https://code.jquery.com/jquery.js"></script>
    {{!-- <script src="../../public/assets/js/client.js"></script> --}}
    {{!--  --}}
</head>

</head>

<body>
    {{{ body }}}
    {{!-- if stylesheet refuses to work... I'll work here --}}
    <style>
        #subButton {
            background-color: cadetblue;
        }

        body {
            background-color: salmon;
        }

        .postDiv {
            background-color: seagreen;
            width: 260px;
        }

        .preDiv {
            background-color: sienna;
            width: 260px;
        }

        .burger-holder {

            height: 33px;
            color: silver;
        }
    </style>
    <script>
        // ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ SCRIPT
        //submit button
       //submit button function is not working
       /*
        $("#SubButton").on("submit", function (event) {
            event.preventDefault();
            console.log("test");
            let myBurger = {
                burger_name: $("#input1").val().trim(),       //should it be "submitField?"
                devoured: false
            };
            console.log(JSON.stringify(myBurger));
            //this is not console logging... this function is not working
            // only info being sent is the form into...
            //lets try making this json
            // it is also returning enter (\r) and \n


            $.ajax("/api/burgers", {
                type: "POST",
                data: myBurger
            }).then(
                function () {
                    console.log("posted new burger");
                    location.reload();
                    // this is not happening yet- probably because post did not go thru...
                }
            );

        });
         */
        //should the route be the same as the get, for restfulness?
        // this is not running- form is broken
        // https://developer.mozilla.org/en-US/docs/Learn/Forms/Your_first_form

    //devouring burgers...

    </script>
</body>

</html>